name: Second Workflow
on: workflow_dispatch
jobs:
    test-job:
        runs-on: ubuntu-latest
        steps:
            - name: Print Greeting
              run: echo "Hello World"
            - name: Print Good
              run: echo "Good Bye"    
